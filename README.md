# Remote Hosts Sync - Neovim Plugin

TUI-плагин для Neovim — альтернатива функции Remote Hosts из PhpStorm. Обеспечивает синхронизацию локальных и удалённых файлов через FTP, SFTP, rsync и другие протоколы с удобным TUI-интерфейсом.

## Возможности

- 🔄 Синхронизация файлов с удалёнными серверами (SFTP, FTP, rsync, SCP)
- 📁 Просмотр структуры удалённых директорий в стиле neo-tree
- 🎯 Управление несколькими хостами через конфигурационный файл
- 🚀 Автоматическая синхронизация при сохранении файлов
- 👁️ Фоновый мониторинг изменений файлов с автосинхронизацией
- 🔑 Поддержка аутентификации по паролю и SSH-ключу
- 🔒 Шифрование паролей в конфигурации
- 📊 Сравнение локальных и удалённых файлов (diff)
- 📝 Логирование всех операций
- 🎨 Интуитивный TUI-интерфейс на базе Telescope

## Требования

- Neovim ≥ 0.9
- [plenary.nvim](https://github.com/nvim-lua/plenary.nvim)
- [telescope.nvim](https://github.com/nvim-telescope/telescope.nvim) (опционально, для TUI)
- Системные утилиты: `sftp`, `ftp`, `rsync`, `ssh` (в зависимости от используемых протоколов)
- `sshpass` для аутентификации SFTP по паролю (опционально)
- `lftp` для расширенной работы с FTP (опционально)

## Установка

### С помощью [lazy.nvim](https://github.com/folke/lazy.nvim)

```lua
{
  'kritile/nvim-remote-sync',
  dependencies = {
    'nvim-lua/plenary.nvim',
    'nvim-telescope/telescope.nvim', -- опционально
  },
  config = function()
    require('core').setup({
      auto_sync = false,  -- автоматическая синхронизация при сохранении
      log_file = '.remote_hosts_sync.log',
      config_file = '.remote_hosts_sync.toml',
    })
  end,
}
```

### С помощью [packer.nvim](https://github.com/wbthomason/packer.nvim)

```lua
use {
  'kritile/nvim-remote-sync',
  requires = {
    'nvim-lua/plenary.nvim',
    'nvim-telescope/telescope.nvim',
  },
  config = function()
    require('core').setup()
  end,
}
```

## Конфигурация

Создайте файл `.remote_hosts_sync.toml` в корне вашего проекта:

```toml
[[hosts]]
name = "Production Server"
type = "sftp"              # ftp | sftp | rsync
host = "example.com"
port = 22
user = "deploy"
password = "secret"        # или путь к SSH-ключу: /home/user/.ssh/id_rsa
path = "/var/www/project"
excludes_local = ["node_modules/", ".git/", "*.log"]
excludes_remote = ["cache/", "logs/"]

[[hosts]]
name = "Staging Server"
type = "rsync"
host = "staging.example.com"
port = 22
user = "deploy"
password = "/home/user/.ssh/staging_key"
path = "/var/www/staging"
excludes_local = ["node_modules/", ".git/"]
excludes_remote = ["cache/"]
```

### Параметры конфигурации

- `name` - Имя хоста для отображения
- `type` - Тип подключения: `sftp`, `ftp`, `rsync`, `scp`
- `host` - Адрес удалённого сервера
- `port` - Порт подключения
- `user` - Имя пользователя
- `password` - Пароль или путь к SSH-ключу (для SFTP/rsync/SCP) - автоматически шифруется
- `path` - Путь к директории на удалённом сервере
- `excludes_local` - Паттерны для исключения локальных файлов
- `excludes_remote` - Паттерны для исключения удалённых файлов

## Использование

### Команды

- `:RemoteHosts` - Открыть список хостов и выбрать активный
- `:RemoteOpenTree [host_name]` - Открыть дерево файлов удалённого сервера
- `:RemoteCloseTree` - Закрыть дерево файлов
- `:RemoteUpload [path]` - Загрузить файл/папку на сервер
- `:RemoteDownload <path>` - Скачать файл/папку с сервера
- `:RemoteSync` - Синхронизировать текущий файл с сервером
- `:RemoteFetch [path]` - Скачать все файлы из директории
- `:RemotePush [path]` - Загрузить все файлы из директории на сервер
- `:RemoteAddHost` - Добавить новый хост
- `:RemoteDisconnect` - Отключиться от текущего хоста
- `:RemoteDiff` - Сравнить текущий файл с удалённой версией (split view)
- `:RemoteDiffPreview` - Показать diff в плавающем окне
- `:RemoteWatchStart [interval]` - Запустить фоновую синхронизацию (интервал в мс, по умолчанию 2000)
- `:RemoteWatchStop` - Остановить фоновую синхронизацию
- `:RemoteWatchToggle [interval]` - Переключить фоновую синхронизацию
- `:RemoteWatchStatus` - Показать статус фоновой синхронизации

### Горячие клавиши в дереве файлов

- `<Enter>` - Открыть/скачать файл или войти в директорию
- `r` - Обновить содержимое текущей директории
- `d` - Скачать выбранный файл/папку
- `u` - Загрузить текущий файл на сервер
- `q` - Закрыть окно дерева файлов

### Горячие клавиши в списке хостов (Telescope)

- `<Enter>` - Подключиться к выбранному хосту
- `<C-e>` - Редактировать конфигурацию хоста
- `<C-d>` - Удалить хост
- `<C-t>` - Проверить соединение с хостом

## Примеры использования

### Базовый workflow

1. Откройте список хостов:
   ```vim
   :RemoteHosts
   ```

2. Выберите нужный хост из списка (Enter)

3. Откройте дерево удалённых файлов:
   ```vim
   :RemoteOpenTree
   ```

4. Навигируйте по файлам и скачивайте нужные

5. Редактируйте файлы локально и синхронизируйте:
   ```vim
   :RemoteSync
   ```

### Автоматическая синхронизация

Включите автоматическую синхронизацию в конфигурации:

```lua
require('core').setup({
  auto_sync = true,
})
```

Теперь файлы будут автоматически загружаться на сервер при сохранении (`:w`).

### Фоновая синхронизация с мониторингом

Запустите фоновый мониторинг изменений:

```vim
:RemoteWatchStart
```

Плагин будет автоматически отслеживать изменения файлов и синхронизировать их каждые 2 секунды (по умолчанию).

Установите свой интервал (в миллисекундах):

```vim
:RemoteWatchStart 5000  " проверка каждые 5 секунд
```

Проверить статус:

```vim
:RemoteWatchStatus
```

Остановить:

```vim
:RemoteWatchStop
```

### Сравнение файлов

Сравните локальную и удалённую версию файла:

```vim
:RemoteDiff
```

Откроется split view с diff. Или используйте preview в плавающем окне:

```vim
:RemoteDiffPreview
```

### Синхронизация всей директории

```vim
:RemotePush .
```

или

```vim
:RemoteFetch /var/www/project
```

## Безопасность

- Используйте SSH-ключи вместо паролей, где это возможно
- **Пароли автоматически шифруются** при сохранении в конфиг (базовое XOR + base64)
- Шифрование основано на данных машины (hostname + username), поэтому конфиг не переносим между машинами
- Файл `.remote_hosts_sync.toml` содержит конфиденциальные данные - добавьте его в `.gitignore`
- Для FTP рекомендуется использовать FTPS
- Все операции логируются в `.remote_hosts_sync.log`

### Отключение шифрования паролей

Если хотите отключить автоматическое шифрование:

```lua
require('core').setup({
  encrypt_passwords = false,
})
```

## Устранение неполадок

### Ошибки подключения

1. Проверьте корректность учётных данных
2. Убедитесь, что нужные утилиты установлены (`sftp`, `rsync`, etc.)
3. Проверьте логи в `.remote_hosts_sync.log`
4. Используйте `:RemoteHosts` и `<C-t>` для тестирования соединения

### SFTP с паролем не работает

Установите `sshpass`:

```bash
# Ubuntu/Debian
sudo apt install sshpass

# macOS
brew install hudochenkov/sshpass/sshpass

# Arch Linux
sudo pacman -S sshpass
```

### Rsync не синхронизирует

Убедитесь, что SSH-ключ настроен правильно и добавлен на удалённый сервер:

```bash
ssh-copy-id user@host
```

## Лицензия

MIT License - см. [LICENSE](LICENSE)

## Автор

kritile

## Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

## Благодарности

Вдохновлён функцией Remote Hosts из PhpStorm/WebStorm от JetBrains.
